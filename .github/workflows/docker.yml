name: Build and Push Docker Image

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: pedrovsc4
          password: Boavista3@

      - name: Build Docker Image
        run: |
          docker build -t pedrovsc4/${{ github.event.repository.name }}:latest .

      - name: Push Docker Image to Docker Hub
        run: |
          docker push pedrovsc4/${{ github.event.repository.name }}:latest
